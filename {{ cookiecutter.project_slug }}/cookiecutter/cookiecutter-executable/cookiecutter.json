{% raw %}
{
  "exec_name": "ExecTemplate",
  "exec_slug": "{{ cookiecutter.exec_name.lower().replace(' ', '_') | replace('-', '_') }}",
  "upper_exec_slug": "{{ cookiecutter.exec_slug.upper() }}"
}
{% endraw %}